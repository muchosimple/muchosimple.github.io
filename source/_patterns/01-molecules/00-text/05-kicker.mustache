<div class="kicker"><a itemprop="articleSection" href="{{ url }}">{{ kicker }}</a> {{# sub-kicker }} <span class="divider">/</span> <a itemprop="articleSection" href="{{ url }}">{{ sub-kicker }}</a> {{/ sub-kicker }} {{# level-3-kicker }} <span class="divider">/</span> <a itemprop="articleSection" href="{{ url }}">{{ level-3-kicker }}</a> {{/ level-3-kicker }}</div>
